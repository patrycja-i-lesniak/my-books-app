import PropTypes from 'prop-types';
import { LoaderContainer, Load } from './styled.js';

const LoaderSmall = () => (
  <LoaderContainer>
    <Load />
  </LoaderContainer>
);

export default LoaderSmall;
